export enum ProductCategory {
  BASE_PRODUCT = 'base',
  ADD_ON = 'add-on',
  MISC_PRODUCT = 'misc',
}

export enum ProductRateChargeType {
  ONE_TIME = 'one-time',
  RECURRING = 'recurring',
  USAGE = 'usage',
}

export enum ProductCurrency {
  USD = 'USD',
}
